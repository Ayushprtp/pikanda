name: pikanda
description: "Don't Forget Me Bae... I Will Miss Uhh...<3"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.7+1

environment:
  sdk: ^3.8.1
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  local_auth: ^2.3.0
  flutter_native_splash: ^2.4.6
  introduction_screen: ^3.1.17
  supabase_flutter: ^2.8.4
  hugeicons: ^0.0.1
#  record: ^6.0.0
  video_player: ^2.9.5
  shared_preferences: ^2.5.3
  just_waveform: ^0.0.7
  internet_connection_checker: ^3.0.1
  just_audio: ^0.10.3
  flip_card: ^0.7.0
  flutter_cache_manager: ^3.4.1
  curved_navigation_bar: ^1.0.6
  firebase_core: ^3.13.0
  cached_network_image: ^3.4.0
  path_provider: ^2.1.2
  http: ^1.2.1
  awesome_snackbar_content: ^0.1.6
  audio_service: ^0.18.17
  permission_handler: ^11.0.1
  dio: ^5.8.0+1
  intl: ^0.20.2
  file_picker: ^10.2.0
  youtube_explode_dart:
#  ^2.4.2
    git:
      url: https://github.com/anandnet/youtube_explode_dart.git
      ref: 1d9ec9baa806705b1d859260eeb389ec28c6b024
  just_audio_media_kit:
    git:
      url: https://github.com/anandnet/just_audio_media_kit.git
      ref: 06de226da469f5c55dd780b215bcc45a0d6269fb
  slide_countdown: ^2.0.2
  device_info_plus: ^11.3.0
  url_launcher: ^6.3.1
#  device_preview: ^1.2.0
  flex_color_picker: ^3.7.1
  share_plus: ^11.0.0
  timezone: ^0.10.1
  flutter_timezone: ^4.1.0
#  flutter_local_notifications: ^19.2.1
  image_picker: ^1.1.2
#  flutter_phone_direct_caller: ^2.2.1
#  background_sms: ^0.0.4
  geolocator: ^14.0.1
  geocoding: ^4.0.0
#  flutter_background_geolocation: ^4.16.10
#  flutter_background_service_android: ^6.3.0
#  flutter_background_service_ios: ^5.0.3
  vibration: ^3.1.3
  mobile_scanner: ^7.0.0
#  contacts_service: ^0.6.3
  audio_waveforms: ^1.3.0
  flutter_background_service: ^5.1.0
#  flutter_onedrive: ^1.7.0
dev_dependencies:
  flutter_launcher_icons: "^0.14.4"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "iassets/images/icon.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "assets/images/icon.png"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/images/icon.png"
#  flutter_native_splash: ^2.0.5

  flutter_test:
    sdk: flutter


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - shorebird.yaml
    - assets/videos/
    - assets/fonts/
    - assets/card/
    - assets/fonts/
    - assets/images/
    - assets/ios/
    - assets/images/avatar/
    - assets/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: SF
      fonts:
        - asset: assets/fonts/SF-PRO-ROUNDED-BOLD.OTF
          weight: 900
        - asset: assets/fonts/SF-PRO-ROUNDED-LIGHT.OTF
          weight: 100
        - asset: assets/fonts/SF-PRO-ROUNDED-MEDIUM.OTF
          weight: 500
        - asset: assets/fonts/SF-PRO-ROUNDED-REGULAR.OTF
          style: normal
    - family: Blanka
      fonts:
        - asset: assets/fonts/Blanka.otf
    - family: Ethnocentric
      fonts:
        - asset: assets/fonts/Ethnocentric.otf
    - family: Jasmine
      fonts:
        - asset: assets/fonts/Jasmine.ttf
    - family: Unitedlovehello
      fonts:
        - asset: assets/fonts/unitedlovehello.ttf
    - family: Thisfeelings
      fonts:
        - asset: assets/fonts/thisfeelings.ttf
    - family: Tamalikamerge
      fonts:
        - asset: assets/fonts/tamalikamerge.ttf
    - family: Skylight
      fonts:
        - asset: assets/fonts/skylight.ttf
    - family: Qualityoflove
      fonts:
        - asset: assets/fonts/qualityoflove.ttf
    - family: Purplemystery
      fonts:
        - asset: assets/fonts/purplemystery.ttf
    - family: Pandastudio
      fonts:
        - asset: assets/fonts/pandastudio.ttf
    - family: Pandalovelybaby
      fonts:
        - asset: assets/fonts/pandalovelybaby.ttf
    - family: Monkeyact
      fonts:
        - asset: assets/fonts/monkeyact.otf
    - family: Feelwithme
      fonts:
        - asset: assets/fonts/feelwithme.ttf
    - family: Faisaljnnkyaw
      fonts:
        - asset: assets/fonts/faisaljnnkyaw.ttf
    - family: Kaushanscript
      fonts:
        - asset: assets/fonts/kaushanscript.ttf
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
